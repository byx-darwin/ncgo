#!/usr/bin/env node
/**
 * Minimal statusline stub for claude-flow.
 *
 * This is a placeholder that outputs a minimal status line.
 * Replace with the real implementation via claude-flow installer.
 */

// Output a minimal status line segment.
// The real implementation would show agent/task status.
process.stdout.write('ncgo');
process.exit(0);
