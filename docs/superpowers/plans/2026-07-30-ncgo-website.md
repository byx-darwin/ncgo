# ncgo 官网 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `website/` 子目录构建 Docusaurus 中英双语官网（落地页 + 文档站），部署到 GitHub Pages，与 Go 代码完全隔离。

**Architecture:** Docusaurus 3 SSG，React 18 + TypeScript 编写落地页交互组件（活终端 / 生长目录树 / 命令 Tabs），Vitest 做组件测试；文档用 markdown（EN 默认 locale + zh-CN）；动画全部用原生 CSS + React state + IntersectionObserver（零动画库，SSR 安全、`prefers-reduced-motion` 降级）；GitHub Actions 独立工作流构建并部署到 Pages。

**Tech Stack:** Docusaurus ^3.8 · React ^18.3 · TypeScript ^5 · Vitest ^3 + @testing-library/react · @fontsource 自托管字体（Chakra Petch / IBM Plex Sans / JetBrains Mono）· GitHub Pages

## Global Constraints

- 站点源码**只允许**位于 `website/`；不得改动 Go 代码、`internal/assets/_data/`、以及现有 `.github/workflows/ci.yml` / `release.yml`。
- 每个任务完成后必须保证 `go build ./...` 与 `./scripts/smoke.sh` 仍通过（website/ 不干扰 Go）。
- 默认 locale 为 `en`，第二 locale 为 `zh-CN`，navbar 带语言切换下拉。
- 文档：EN 位于 `website/docs/`，中文位于 `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/`，两边信息架构严格对齐。
- `docusaurus.config.js` 必须设置 `onBrokenLinks: 'throw'`、`onBrokenMarkdownLinks: 'throw'`；`baseUrl: '/ncgo/'`、`url: 'https://byx-darwin.github.io'`。
- 字体通过 `@fontsource` 自托管，禁止引用外部字体 CDN。
- 配色：深蓝墨底 + Go 青 `#00ADD8` 主色 + 琥珀金 / 终端绿点缀；禁止 indigo/violet/pink 渐变标题。
- 落地页必须在 JS 关闭时仍显示内容（依赖 Docusaurus SSG 预渲染），并尊重 `prefers-reduced-motion`。
- `website/package.json` 的 `engines` 固定 `node >= 20`。
- 命令块必须来自真实 `ncgo` 输出（用本计划给出的捕获命令），不得凭空编造。

---

## File Structure

```
website/
├── package.json                 # npm 项目，engines node>=20，scripts: start/build/serve/test
├── docusaurus.config.js         # 站点配置：i18n、navbar、footer、theme、baseUrl
├── sidebars.js                  # 文档侧边栏（autogenerated）
├── babel.config.js              # Docusaurus 默认
├── tsconfig.json                # 继承 @docusaurus/tsconfig，strict
├── vitest.config.ts             # Vitest + jsdom + setup
├── vitest.setup.ts              # @testing-library/jest-dom、matchMedia/clipboard mock
├── docs/                        # EN 文档（默认 locale）
│   ├── intro.md
│   ├── getting-started/install.md
│   ├── getting-started/30-second-tour.md
│   ├── guides/new-service.md
│   ├── guides/micro-workspace.md
│   ├── guides/domain-method.md
│   ├── guides/infra.md
│   ├── guides/ai-collaboration.md
│   ├── guides/diagnostics.md
│   ├── reference/commands.md
│   └── faq.md
├── i18n/zh-CN/
│   ├── docusaurus-plugin-content-docs/current/   # zh 文档（镜像 docs/ 结构）
│   └── docusaurus-theme-classic.json             # zh 主题文案
├── src/
│   ├── css/custom.css           # 设计令牌 + 全局样式 + 组件样式 + 动画
│   ├── data/copy.ts             # 落地页双语文案 + 终端脚本 + 目录树数据
│   ├── components/Terminal.tsx
│   ├── components/FileTree.tsx
│   ├── components/CommandTabs.tsx
│   ├── components/CopyCommand.tsx
│   ├── components/Reveal.tsx    # 滚动浮现包装器（useInView）
│   └── components/__tests__/
│       ├── Terminal.test.tsx
│       ├── FileTree.test.tsx
│       ├── CommandTabs.test.tsx
│       └── CopyCommand.test.tsx
├── src/pages/index.tsx          # 落地页（组合各 section）
└── static/
    └── img/favicon.svg

.github/workflows/website.yml    # 新增：Pages 构建 + 部署（独立于 Go CI）
.gitignore                        # 追加 website/node_modules、website/build
README.md / README.zh-CN.md       # 追加官网链接徽章（可选收尾）
```

**责任边界：**
- `src/components/*` 只负责渲染与动画，不含文案；文案全部来自 `src/data/copy.ts`（单一数据源，便于双语与测试）。
- `docs/` 与 `i18n/.../current/` 是纯内容，不含组件逻辑。
- `custom.css` 持有全部设计令牌（CSS variables），组件只消费令牌。

---

## Task 1: 脚手架 + 配置 + 字体 + 首次构建通过

**Files:**
- Create: `website/package.json`, `website/docusaurus.config.js`, `website/sidebars.js`, `website/babel.config.js`, `website/tsconfig.json`, `website/src/css/custom.css`, `website/static/img/favicon.svg`, `website/docs/intro.md`, `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/intro.md`, `website/i18n/zh-CN/docusaurus-theme-classic.json`
- Modify: `.gitignore`（追加 website 忽略项）

**Interfaces:**
- Produces: `website/` 可独立 `npm install && npm run build`；`docusaurus.config.js` 导出含 i18n（en + zh-CN）、`baseUrl '/ncgo/'`、`onBrokenLinks 'throw'`、navbar localeDropdown 的配置。后续任务在此之上加组件与文档。

- [ ] **Step 1: 初始化 website/package.json**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
mkdir -p website
cd website
npm init -y
```

然后用以下内容**完整覆盖** `website/package.json`：

```json
{
  "name": "ncgo-website",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@docusaurus/core": "^3.8.1",
    "@docusaurus/preset-classic": "^3.8.1",
    "@fontsource/chakra-petch": "^5.2.5",
    "@fontsource/ibm-plex-sans": "^5.2.5",
    "@fontsource/jetbrains-mono": "^5.2.5",
    "prism-react-renderer": "^2.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.8.1",
    "@docusaurus/tsconfig": "^3.8.1",
    "@docusaurus/types": "^3.8.1",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "browserslist": {
    "production": [">0.5%", "not dead", "not op_mini all"],
    "development": ["last 3 chrome version", "last 3 firefox version", "last 5 safari version"]
  }
}
```

- [ ] **Step 2: 安装依赖**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm install`
Expected: 成功，生成 `node_modules/` 与 `package-lock.json`，无 peer-dep 报错。

- [ ] **Step 3: 写 docusaurus.config.js（含 i18n / baseUrl / navbar / footer / 字体入口）**

Create `website/docusaurus.config.js`：

```js
// @ts-check
const { themes } = require('prism-react-renderer');

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'ncgo',
  tagline: 'AI-friendly scaffold CLI for Go microservices',
  favicon: 'img/favicon.svg',

  url: 'https://byx-darwin.github.io',
  baseUrl: '/ncgo/',

  organizationName: 'byx-darwin',
  projectName: 'ncgo',

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'throw',

  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'zh-CN'],
    localeConfigs: {
      en: { label: 'English' },
      'zh-CN': { label: '简体中文' },
    },
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          routeBasePath: 'docs',
          sidebarPath: require.resolve('./sidebars.js'),
          editUrl: 'https://github.com/byx-darwin/ncgo/tree/main/website/',
        },
        blog: false,
        theme: { customCss: require.resolve('./src/css/custom.css') },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      colorMode: { defaultMode: 'dark', disableSwitch: false, respectPrefersColorScheme: false },
      navbar: {
        title: 'ncgo',
        logo: { alt: 'ncgo logo', src: 'img/favicon.svg' },
        items: [
          { type: 'docSidebar', sidebarId: 'docs', position: 'left', label: 'Docs' },
          {
            href: 'https://github.com/byx-darwin/ncgo',
            label: 'GitHub',
            position: 'right',
          },
          { type: 'localeDropdown', position: 'right' },
        ],
      },
      footer: {
        style: 'dark',
        links: [
          {
            title: 'Docs',
            items: [{ label: 'Getting Started', to: '/docs/intro' }],
          },
          {
            title: 'Community',
            items: [
              { label: 'GitHub', href: 'https://github.com/byx-darwin/ncgo' },
              { label: 'Issues', href: 'https://github.com/byx-darwin/ncgo/issues' },
            ],
          },
        ],
        copyright: `Copyright © ${new Date().getFullYear()} ncgo authors. Built with Docusaurus.`,
      },
      prism: { theme: themes.github, darkTheme: themes.dracula },
    }),
};

module.exports = config;
```

- [ ] **Step 4: 写 sidebars.js 与 babel.config.js**

Create `website/sidebars.js`：

```js
// @ts-check
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  docs: [{ type: 'autogenerated', dirName: '.' }],
};
module.exports = sidebars;
```

Create `website/babel.config.js`：

```js
module.exports = {
  presets: [require.resolve('@docusaurus/core/lib/babel/preset')],
};
```

- [ ] **Step 5: 写 tsconfig.json**

Create `website/tsconfig.json`：

```json
{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "jsx": "react-jsx",
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "types": ["@docusaurus/module-type-aliases", "@testing-library/jest-dom"]
  },
  "include": ["src", "vitest.setup.ts"],
  "exclude": ["node_modules", "build", ".docusaurus"]
}
```

- [ ] **Step 6: 写 custom.css（设计令牌 + 字体 + 环境背景）**

Create `website/src/css/custom.css`：

```css
/* 自托管字体（@fontsource） */
@import '@fontsource/chakra-petch/500.css';
@import '@fontsource/chakra-petch/700.css';
@import '@fontsource/ibm-plex-sans/400.css';
@import '@fontsource/ibm-plex-sans/500.css';
@import '@fontsource/ibm-plex-sans/600.css';
@import '@fontsource/jetbrains-mono/400.css';
@import '@fontsource/jetbrains-mono/600.css';

:root {
  /* 品牌色 */
  --ncgo-cyan: #00add8;
  --ncgo-cyan-soft: rgba(0, 173, 216, 0.14);
  --ncgo-amber: #ffb454;
  --ncgo-green: #5ee38f;
  --ncgo-ink: #0b1622;          /* 深蓝墨底 */
  --ncgo-ink-2: #0f1f30;
  --ncgo-ink-3: #132a3f;
  --ncgo-text: #e6eef6;
  --ncgo-text-dim: #93a6b8;
  --ncgo-line: rgba(147, 166, 184, 0.16);

  /* Docusaurus 主题变量映射 */
  --ifm-color-primary: var(--ncgo-cyan);
  --ifm-color-primary-dark: #009bc2;
  --ifm-color-primary-darker: #0092b7;
  --ifm-color-primary-darkest: #007897;
  --ifm-color-primary-light: #13c3ea;
  --ifm-color-primary-lighter: #20c8ec;
  --ifm-color-primary-lightest: #41d2f0;
  --ifm-background-color: var(--ncgo-ink);
  --ifm-background-surface-color: var(--ncgo-ink-2);
  --ifm-font-family-base: 'IBM Plex Sans', system-ui, sans-serif;
  --ifm-font-family-monospace: 'JetBrains Mono', ui-monospace, monospace;
  --ifm-heading-font-family: 'Chakra Petch', 'IBM Plex Sans', sans-serif;
  --ifm-code-font-size: 90%;
  --docusaurus-highlighted-code-line-bg: rgba(0, 173, 216, 0.12);
}

[data-theme='light'] {
  --ncgo-ink: #f5f8fb;
  --ncgo-ink-2: #ffffff;
  --ncgo-ink-3: #eaf1f7;
  --ncgo-text: #14212e;
  --ncgo-text-dim: #51677c;
  --ncgo-line: rgba(20, 33, 46, 0.12);
  --ifm-background-color: var(--ncgo-ink);
  --ifm-background-surface-color: var(--ncgo-ink-2);
}

html {
  font-family: var(--ifm-font-family-base);
}

/* 环境背景：细网格 + 双色光晕（非单色霓虹） */
body {
  background-color: var(--ncgo-ink);
  background-image:
    radial-gradient(ellipse 80% 50% at 15% -10%, var(--ncgo-cyan-soft), transparent 60%),
    radial-gradient(ellipse 60% 40% at 90% 0%, rgba(255, 180, 84, 0.08), transparent 60%),
    linear-gradient(var(--ncgo-line) 1px, transparent 1px),
    linear-gradient(90deg, var(--ncgo-line) 1px, transparent 1px);
  background-size: 100% 100%, 100% 100%, 44px 44px, 44px 44px;
  background-attachment: fixed;
  color: var(--ncgo-text);
}

h1, h2, h3, h4 {
  font-family: var(--ifm-heading-font-family);
  letter-spacing: 0.01em;
}
```

- [ ] **Step 7: 写 favicon.svg（Go 青终端符号）**

Create `website/static/img/favicon.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="12" fill="#0b1622"/>
  <path d="M18 22l10 10-10 10" fill="none" stroke="#00add8" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M34 44h12" stroke="#ffb454" stroke-width="5" stroke-linecap="round"/>
</svg>
```

- [ ] **Step 8: 写最小 docs（EN + zh）与主题文案，保证有内容可构建**

Create `website/docs/intro.md`：

```md
---
sidebar_position: 1
slug: /intro
title: Introduction
---

# ncgo

`ncgo` is an AI-friendly scaffold CLI for Go microservices. It generates
Hertz (HTTP) and Kitex (RPC) service scaffolds, renders AI context files
(`AGENTS.md`, `CLAUDE.md`, Cursor rules), and exposes operations via both CLI
and an MCP stdio server.

## Why ncgo

- **Deterministic scaffolding** — manifests, IDL placeholders, and templates stay under version control.
- **Generator-aware** — orchestrates `hz` / `kitex` while supporting `--no-generate`.
- **Agent-friendly by default** — renders AI context files and exposes MCP tools.
- **Lifecycle helpers** — `doctor`, `upgrade`, and conservative `extract domain`.
```

Create `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/intro.md`：

```md
---
sidebar_position: 1
slug: /intro
title: 简介
---

# ncgo

`ncgo` 是面向 Go 微服务的 AI 友好脚手架 CLI。它生成 Hertz（HTTP）与 Kitex（RPC）服务骨架，
渲染 AI 上下文文件（`AGENTS.md`、`CLAUDE.md`、Cursor 规则），并通过 CLI 与 MCP stdio 服务器暴露操作。

## 为什么选择 ncgo

- **确定性脚手架** —— manifest、IDL 占位符与模板全部纳入版本控制。
- **感知生成器** —— 编排 `hz` / `kitex`，同时支持 `--no-generate`。
- **默认对 Agent 友好** —— 渲染 AI 上下文文件并暴露 MCP 工具。
- **生命周期助手** —— 内置 `doctor`、`upgrade` 与保守的 `extract domain`。
```

生成并填充主题文案（用 `write-translations` 生成真实键，再补中文，避免键名不匹配）：

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website
npm run write-translations -- --locale zh-CN
node -e "
const fs = require('fs');
const p = 'i18n/zh-CN/docusaurus-theme-classic.json';
const j = JSON.parse(fs.readFileSync(p, 'utf8'));
if (j['item.label.Docs']) j['item.label.Docs'].message = '文档';
if (j['link.item.label.Getting Started']) j['link.item.label.Getting Started'].message = '快速开始';
if (j['link.item.label.GitHub']) j['link.item.label.GitHub'].message = 'GitHub';
if (j['link.item.label.Issues']) j['link.item.label.Issues'].message = '议题';
if (j['theme.docs.sidebar.collapseButtonTitle']) j['theme.docs.sidebar.collapseButtonTitle'].message = '收起侧边栏';
if (j['theme.docs.sidebar.expandButtonTitle']) j['theme.docs.sidebar.expandButtonTitle'].message = '展开侧边栏';
fs.writeFileSync(p, JSON.stringify(j, null, 2));
console.log('theme-classic.json patched');
"
```

Expected: 输出 `theme-classic.json patched`，且该文件含 `item.label.Docs` 等键。

- [ ] **Step 9: 更新 .gitignore**

在 `.gitignore` 末尾追加：

```
# Website (Docusaurus)
website/node_modules/
website/build/
website/.docusaurus/
```

- [ ] **Step 10: 构建两个 locale 验证**

Run:
```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website
npm run build
npm run build -- --locale zh-CN
```
Expected: 两条命令均以 `SUCCESS` 结束，`build/` 下生成静态站点，无 broken link 报错。

- [ ] **Step 11: 确认 Go 侧不受影响**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && go build ./...`
Expected: 成功，无输出。

- [ ] **Step 12: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website .gitignore
git commit -m "feat(website): scaffold Docusaurus site with i18n and theme foundation"
```

---

## Task 2: Vitest 测试基建

**Files:**
- Create: `website/vitest.config.ts`, `website/vitest.setup.ts`

**Interfaces:**
- Consumes: Task 1 的 `website/`（package.json 已含 vitest 依赖与 `test` script）。
- Produces: `npm test` 可运行；后续组件测试依赖 `vitest.setup.ts` 提供的 `matchMedia` / `clipboard` / `IntersectionObserver` mock。

- [ ] **Step 1: 写 vitest.config.ts**

Create `website/vitest.config.ts`：

```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
    css: false,
  },
});
```

- [ ] **Step 2: 添加 @vitejs/plugin-react 依赖**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm install -D @vitejs/plugin-react@^4.3.4`
Expected: 成功写入 devDependencies。

- [ ] **Step 3: 写 vitest.setup.ts（全局 mock）**

Create `website/vitest.setup.ts`：

```ts
import '@testing-library/jest-dom/vitest';
import { vi } from 'vitest';

// jsdom 无 matchMedia：默认「不减少动效」，测试可覆盖
if (typeof window !== 'undefined' && !window.matchMedia) {
  Object.defineProperty(window, 'matchMedia', {
    writable: true,
    value: vi.fn().mockImplementation((query: string) => ({
      matches: false,
      media: query,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    })),
  });
}

// jsdom 无 navigator.clipboard
Object.defineProperty(navigator, 'clipboard', {
  writable: true,
  value: { writeText: vi.fn().mockResolvedValue(undefined) },
});

// jsdom 无 IntersectionObserver：默认不相交（Reveal 组件应降级为可见）
class MockIntersectionObserver {
  observe = vi.fn();
  unobserve = vi.fn();
  disconnect = vi.fn();
}
if (typeof window !== 'undefined' && !('IntersectionObserver' in window)) {
  Object.defineProperty(window, 'IntersectionObserver', {
    writable: true,
    value: MockIntersectionObserver,
  });
  Object.defineProperty(globalThis, 'IntersectionObserver', {
    writable: true,
    value: MockIntersectionObserver,
  });
}
```

- [ ] **Step 4: 写一个冒烟测试验证测试基建**

Create `website/src/components/__tests__/smoke.test.ts`：

```ts
import { describe, expect, it } from 'vitest';

describe('test infra', () => {
  it('runs in jsdom with mocks', () => {
    expect(typeof window).toBe('object');
    expect(window.matchMedia('(prefers-reduced-motion: reduce)').matches).toBe(false);
    expect(navigator.clipboard.writeText).toBeDefined();
  });
});
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm test`
Expected: `1 passed`。

- [ ] **Step 6: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/vitest.config.ts website/vitest.setup.ts website/src/components/__tests__/smoke.test.ts website/package.json website/package-lock.json
git commit -m "test(website): add Vitest infra with jsdom mocks"
```

---

## Task 3: Terminal 组件（TDD）

**Files:**
- Create: `website/src/components/Terminal.tsx`, `website/src/components/__tests__/Terminal.test.tsx`

**Interfaces:**
- Produces: 默认导出 `Terminal({ lines, title?, className? })`；具名导出类型 `TermLine = { kind: 'command'|'output'|'success'|'highlight'|'blank'; text: string }`。供 `index.tsx` 与 `CommandTabs` 复用。
- 行为契约：SSR / 无 JS / reduced-motion 时一次性渲染全部行；客户端动画逐行出现；command 行带 `$` 前缀。

- [ ] **Step 1: 写失败测试**

Create `website/src/components/__tests__/Terminal.test.tsx`：

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest';
import Terminal, { TermLine } from '../Terminal';

const lines: TermLine[] = [
  { kind: 'command', text: 'ncgo new user-api' },
  { kind: 'output', text: 'manifest.yaml' },
  { kind: 'highlight', text: 'AGENTS.md' },
];

describe('Terminal', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('renders all lines when reduced motion is preferred', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    render(<Terminal lines={lines} />);
    expect(screen.getByText('ncgo new user-api')).toBeInTheDocument();
    expect(screen.getByText('manifest.yaml')).toBeInTheDocument();
    expect(screen.getByText('AGENTS.md')).toBeInTheDocument();
  });

  it('eventually reveals all lines when animating', () => {
    render(<Terminal lines={lines} />);
    // 推进足够长的时间让递归 timeout 完成
    vi.advanceTimersByTime(60_000);
    expect(screen.getByText('ncgo new user-api')).toBeInTheDocument();
    expect(screen.getByText('manifest.yaml')).toBeInTheDocument();
    expect(screen.getByText('AGENTS.md')).toBeInTheDocument();
  });

  it('prefixes command lines with $ prompt', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    const { container } = render(<Terminal lines={lines} />);
    expect(container.querySelector('.term-prompt')?.textContent).toBe('$ ');
  });

  it('applies kind classes to lines', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    const { container } = render(<Terminal lines={lines} />);
    expect(container.querySelectorAll('.term-command').length).toBeGreaterThan(0);
    expect(container.querySelectorAll('.term-highlight').length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/Terminal.test.tsx`
Expected: FAIL（`Terminal` 模块不存在 / 无法解析）。

- [ ] **Step 3: 实现 Terminal.tsx**

Create `website/src/components/Terminal.tsx`：

```tsx
import React, { useEffect, useRef, useState } from 'react';

export type TermLine = {
  kind: 'command' | 'output' | 'success' | 'highlight' | 'blank';
  text: string;
};

type Props = {
  lines: TermLine[];
  title?: string;
  className?: string;
};

const KIND_CLASS: Record<TermLine['kind'], string> = {
  command: 'term-command',
  output: 'term-output',
  success: 'term-success',
  highlight: 'term-highlight',
  blank: 'term-blank',
};

function prefersReducedMotion(): boolean {
  return (
    typeof window !== 'undefined' &&
    typeof window.matchMedia === 'function' &&
    window.matchMedia('(prefers-reduced-motion: reduce)').matches
  );
}

export default function Terminal({ lines, title = 'terminal', className = '' }: Props) {
  const [mounted, setMounted] = useState(false);
  const [visible, setVisible] = useState(lines.length);
  const [partial, setPartial] = useState('');
  const timers = useRef<number[]>([]);

  useEffect(() => setMounted(true), []);
  const animate = mounted && !prefersReducedMotion();

  useEffect(() => {
    if (!animate) return;
    const clearAll = () => {
      timers.current.forEach((t) => window.clearTimeout(t));
      timers.current = [];
    };
    clearAll();
    setVisible(0);
    setPartial('');

    let line = 0;
    const schedule = (fn: () => void, delay: number) => {
      timers.current.push(window.setTimeout(fn, delay));
    };

    const process = () => {
      if (line >= lines.length) return;
      const cur = lines[line];
      if (cur.kind === 'command') {
        let char = 0;
        const typeChar = () => {
          char += 2;
          if (char >= cur.text.length) {
            setPartial('');
            setVisible(line + 1);
            line += 1;
            schedule(process, 160);
          } else {
            setPartial(cur.text.slice(0, char));
            schedule(typeChar, 26);
          }
        };
        schedule(typeChar, 60);
      } else {
        setVisible(line + 1);
        line += 1;
        schedule(process, cur.kind === 'blank' ? 40 : 100);
      }
    };
    schedule(process, 350);
    return clearAll;
  }, [animate, lines]);

  const rows: React.ReactNode[] = [];
  for (let i = 0; i < Math.min(visible, lines.length); i++) {
    const l = lines[i];
    rows.push(
      <div key={`l-${i}`} className={`term-line ${KIND_CLASS[l.kind]}`}>
        {l.kind === 'command' ? <span className="term-prompt">$ </span> : null}
        {l.text}
      </div>,
    );
  }
  if (partial) {
    rows.push(
      <div key="partial" className="term-line term-command">
        <span className="term-prompt">$ </span>
        {partial}
        <span className="term-caret" aria-hidden="true" />
      </div>,
    );
  }

  return (
    <div className={`terminal ${className}`.trim()}>
      <div className="terminal-bar">
        <span className="terminal-dot terminal-dot-r" />
        <span className="terminal-dot terminal-dot-y" />
        <span className="terminal-dot terminal-dot-g" />
        <span className="terminal-title">{title}</span>
      </div>
      <div className="terminal-body" role="log" aria-live="polite">
        {rows}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/Terminal.test.tsx`
Expected: `4 passed`。

- [ ] **Step 5: 添加 Terminal 样式到 custom.css**

在 `website/src/css/custom.css` 末尾追加：

```css
/* ── Terminal ─────────────────────────────── */
.terminal {
  border: 1px solid var(--ncgo-line);
  border-radius: 10px;
  background: color-mix(in srgb, var(--ncgo-ink-2) 92%, transparent);
  box-shadow: 0 18px 48px rgba(2, 10, 18, 0.45);
  overflow: hidden;
}
.terminal-bar {
  display: flex;
  align-items: center;
  gap: 7px;
  padding: 10px 14px;
  border-bottom: 1px solid var(--ncgo-line);
  background: var(--ncgo-ink-3);
}
.terminal-dot { width: 11px; height: 11px; border-radius: 50%; }
.terminal-dot-r { background: #ff5f56; }
.terminal-dot-y { background: #ffbd2e; }
.terminal-dot-g { background: #27c93f; }
.terminal-title {
  margin-left: 8px;
  font-family: var(--ifm-font-family-monospace);
  font-size: 12px;
  color: var(--ncgo-text-dim);
}
.terminal-body {
  padding: 16px 18px;
  font-family: var(--ifm-font-family-monospace);
  font-size: 13.5px;
  line-height: 1.7;
}
.term-line { white-space: pre-wrap; }
.term-command { color: var(--ncgo-text); font-weight: 600; }
.term-prompt { color: var(--ncgo-cyan); }
.term-output { color: var(--ncgo-text-dim); }
.term-success { color: var(--ncgo-green); }
.term-highlight { color: var(--ncgo-amber); text-shadow: 0 0 14px rgba(255, 180, 84, 0.35); }
.term-caret {
  display: inline-block;
  width: 8px;
  height: 1.05em;
  margin-left: 2px;
  vertical-align: text-bottom;
  background: var(--ncgo-cyan);
  animation: term-blink 1s steps(1) infinite;
}
@keyframes term-blink { 50% { opacity: 0; } }
@media (prefers-reduced-motion: reduce) {
  .term-caret { animation: none; }
}
```

- [ ] **Step 6: 类型检查**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run typecheck`
Expected: 无错误。

- [ ] **Step 7: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/src/components/Terminal.tsx website/src/components/__tests__/Terminal.test.tsx website/src/css/custom.css
git commit -m "feat(website): add animated Terminal component with tests"
```

---

## Task 4: FileTree + CopyCommand + CommandTabs 组件（TDD）

**Files:**
- Create: `website/src/components/FileTree.tsx`, `website/src/components/CopyCommand.tsx`, `website/src/components/CommandTabs.tsx`
- Create: `website/src/components/__tests__/FileTree.test.tsx`, `CopyCommand.test.tsx`, `CommandTabs.test.tsx`

**Interfaces:**
- Consumes: `Terminal`（Task 3）。
- Produces:
  - `FileTree({ rows: TreeRow[] })`，`TreeRow = { text: string; depth: number; ai?: boolean }`；SSR 安全、逐行生长。
  - `CopyCommand({ command: string; label?: string })`；点击复制 `command` 到剪贴板并短暂显示 ✓。
  - `CommandTabs({ tabs: CommandTab[] })`，`CommandTab = { id: string; label: string; lines: TermLine[] }`；切换 tab 渲染对应 Terminal。

- [ ] **Step 1: 写 FileTree 失败测试**

Create `website/src/components/__tests__/FileTree.test.tsx`：

```tsx
import { render } from '@testing-library/react';
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest';
import FileTree, { TreeRow } from '../FileTree';

const rows: TreeRow[] = [
  { text: 'user-api/', depth: 0 },
  { text: 'manifest.yaml', depth: 1 },
  { text: '.claude/', depth: 1, ai: true },
];

describe('FileTree', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('renders all rows when reduced motion is preferred', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    const { container } = render(<FileTree rows={rows} />);
    expect(container.querySelectorAll('.filetree-row').length).toBe(3);
  });

  it('reveals rows over time when animating', () => {
    const { container } = render(<FileTree rows={rows} />);
    vi.advanceTimersByTime(5_000);
    expect(container.querySelectorAll('.filetree-row').length).toBe(3);
  });

  it('marks ai rows with badge', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    const { container } = render(<FileTree rows={rows} />);
    expect(container.querySelectorAll('.filetree-ai').length).toBe(1);
    expect(container.querySelector('.filetree-badge')?.textContent).toBe('AI');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/FileTree.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 FileTree.tsx**

Create `website/src/components/FileTree.tsx`：

```tsx
import React, { useEffect, useState } from 'react';

export type TreeRow = { text: string; depth: number; ai?: boolean };

function prefersReducedMotion(): boolean {
  return (
    typeof window !== 'undefined' &&
    typeof window.matchMedia === 'function' &&
    window.matchMedia('(prefers-reduced-motion: reduce)').matches
  );
}

export default function FileTree({ rows, className = '' }: { rows: TreeRow[]; className?: string }) {
  const [mounted, setMounted] = useState(false);
  const [visible, setVisible] = useState(rows.length);

  useEffect(() => setMounted(true), []);
  const animate = mounted && !prefersReducedMotion();

  useEffect(() => {
    if (!animate) return;
    setVisible(0);
    const timers = rows.map((_, i) =>
      window.setTimeout(() => setVisible(i + 1), 130 * (i + 1)),
    );
    return () => timers.forEach((t) => window.clearTimeout(t));
  }, [animate, rows]);

  return (
    <div className={`filetree ${className}`.trim()}>
      {rows.slice(0, visible).map((r, i) => (
        <div
          key={`r-${i}`}
          className={`filetree-row ${r.ai ? 'filetree-ai' : ''}`.trim()}
          style={{ paddingLeft: `${r.depth * 18 + 14}px` }}
        >
          <span className="filetree-text">{r.text}</span>
          {r.ai ? <span className="filetree-badge">AI</span> : null}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: 运行 FileTree 测试确认通过**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/FileTree.test.tsx`
Expected: `3 passed`。

- [ ] **Step 5: 写 CopyCommand 失败测试**

Create `website/src/components/__tests__/CopyCommand.test.tsx`：

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { describe, expect, it, vi } from 'vitest';
import CopyCommand from '../CopyCommand';

describe('CopyCommand', () => {
  it('shows the command text', () => {
    render(<CopyCommand command="go install github.com/byx-darwin/ncgo@latest" />);
    expect(screen.getByText('go install github.com/byx-darwin/ncgo@latest')).toBeInTheDocument();
  });

  it('copies command to clipboard on click', async () => {
    const user = userEvent.setup();
    const cmd = 'go install github.com/byx-darwin/ncgo@latest';
    render(<CopyCommand command={cmd} />);
    await user.click(screen.getByRole('button', { name: 'copy' }));
    expect(navigator.clipboard.writeText).toHaveBeenCalledWith(cmd);
  });
});
```

- [ ] **Step 6: 运行确认失败**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/CopyCommand.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 7: 实现 CopyCommand.tsx**

Create `website/src/components/CopyCommand.tsx`：

```tsx
import React, { useState } from 'react';

export default function CopyCommand({ command, label = 'copy' }: { command: string; label?: string }) {
  const [copied, setCopied] = useState(false);

  const onCopy = async () => {
    try {
      await navigator.clipboard.writeText(command);
      setCopied(true);
      window.setTimeout(() => setCopied(false), 1600);
    } catch {
      setCopied(false);
    }
  };

  return (
    <div className="copy-command">
      <code className="copy-command-text">{command}</code>
      <button type="button" className="copy-command-btn" onClick={onCopy} aria-label={label}>
        {copied ? '✓' : '⧉'}
      </button>
    </div>
  );
}
```

- [ ] **Step 8: 运行 CopyCommand 测试确认通过**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/CopyCommand.test.tsx`
Expected: `2 passed`。

- [ ] **Step 9: 写 CommandTabs 失败测试**

Create `website/src/components/__tests__/CommandTabs.test.tsx`：

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest';
import CommandTabs, { CommandTab } from '../CommandTabs';

const tabs: CommandTab[] = [
  { id: 'doctor', label: 'doctor', lines: [{ kind: 'command', text: 'ncgo doctor' }, { kind: 'output', text: 'hz: ok' }] },
  { id: 'infra', label: 'add infra', lines: [{ kind: 'command', text: 'ncgo add infra redis' }, { kind: 'output', text: 'redis: added' }] },
];

describe('CommandTabs', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('shows first tab content by default', () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    render(<CommandTabs tabs={tabs} />);
    expect(screen.getByText('ncgo doctor')).toBeInTheDocument();
  });

  it('switches content when a tab is clicked', async () => {
    window.matchMedia = vi.fn().mockImplementation((q: string) => ({
      matches: q.includes('reduce'),
      media: q,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }));
    const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
    render(<CommandTabs tabs={tabs} />);
    await user.click(screen.getByRole('tab', { name: 'add infra' }));
    expect(screen.getByText('ncgo add infra redis')).toBeInTheDocument();
  });
});
```

- [ ] **Step 10: 运行确认失败**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/CommandTabs.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 11: 实现 CommandTabs.tsx**

Create `website/src/components/CommandTabs.tsx`：

```tsx
import React, { useState } from 'react';
import Terminal, { TermLine } from './Terminal';

export type CommandTab = { id: string; label: string; lines: TermLine[] };

export default function CommandTabs({ tabs }: { tabs: CommandTab[] }) {
  const [active, setActive] = useState<string | undefined>(tabs[0]?.id);
  const current = tabs.find((t) => t.id === active) ?? tabs[0];

  return (
    <div className="command-tabs">
      <div role="tablist" className="command-tabs-list">
        {tabs.map((t) => (
          <button
            key={t.id}
            role="tab"
            type="button"
            aria-selected={t.id === active}
            className={`command-tab ${t.id === active ? 'command-tab-active' : ''}`.trim()}
            onClick={() => setActive(t.id)}
          >
            {t.label}
          </button>
        ))}
      </div>
      {current ? <Terminal lines={current.lines} title={current.label} /> : null}
    </div>
  );
}
```

- [ ] **Step 12: 运行 CommandTabs 测试确认通过**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npx vitest run src/components/__tests__/CommandTabs.test.tsx`
Expected: `2 passed`。

- [ ] **Step 13: 追加 FileTree / CopyCommand / CommandTabs 样式**

在 `website/src/css/custom.css` 末尾追加：

```css
/* ── FileTree ─────────────────────────────── */
.filetree {
  border: 1px solid var(--ncgo-line);
  border-radius: 10px;
  background: color-mix(in srgb, var(--ncgo-ink-2) 92%, transparent);
  padding: 12px 4px;
  font-family: var(--ifm-font-family-monospace);
  font-size: 13px;
  line-height: 1.9;
}
.filetree-row { display: flex; align-items: center; gap: 8px; }
.filetree-text { color: var(--ncgo-text-dim); }
.filetree-ai .filetree-text { color: var(--ncgo-amber); }
.filetree-badge {
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.08em;
  color: var(--ncgo-ink);
  background: var(--ncgo-amber);
  border-radius: 4px;
  padding: 1px 5px;
}

/* ── CopyCommand ──────────────────────────── */
.copy-command {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  border: 1px solid var(--ncgo-line);
  border-radius: 8px;
  background: var(--ncgo-ink-3);
  padding: 10px 8px 10px 16px;
}
.copy-command-text {
  font-family: var(--ifm-font-family-monospace);
  font-size: 14px;
  color: var(--ncgo-text);
}
.copy-command-btn {
  border: none;
  background: var(--ncgo-cyan-soft);
  color: var(--ncgo-cyan);
  border-radius: 6px;
  width: 32px;
  height: 32px;
  cursor: pointer;
  font-size: 15px;
  transition: transform 0.15s ease, background 0.15s ease;
}
.copy-command-btn:hover { transform: translateY(-1px); background: rgba(0, 173, 216, 0.24); }

/* ── CommandTabs ──────────────────────────── */
.command-tabs-list {
  display: flex;
  gap: 6px;
  margin-bottom: -1px;
  position: relative;
  z-index: 1;
}
.command-tab {
  border: 1px solid var(--ncgo-line);
  border-bottom: none;
  background: transparent;
  color: var(--ncgo-text-dim);
  font-family: var(--ifm-font-family-monospace);
  font-size: 13px;
  padding: 8px 16px;
  border-radius: 8px 8px 0 0;
  cursor: pointer;
  transition: color 0.15s ease, background 0.15s ease;
}
.command-tab:hover { color: var(--ncgo-cyan); }
.command-tab-active {
  background: var(--ncgo-ink-3);
  color: var(--ncgo-cyan);
  font-weight: 600;
}
```

- [ ] **Step 14: 类型检查 + 全量测试**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run typecheck && npm test`
Expected: typecheck 无错误；全部测试通过。

- [ ] **Step 15: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/src/components/FileTree.tsx website/src/components/CopyCommand.tsx website/src/components/CommandTabs.tsx \
        website/src/components/__tests__/FileTree.test.tsx website/src/components/__tests__/CopyCommand.test.tsx website/src/components/__tests__/CommandTabs.test.tsx \
        website/src/css/custom.css
git commit -m "feat(website): add FileTree, CopyCommand, CommandTabs components with tests"
```

---

## Task 5: Reveal（滚动浮现）+ 落地页文案数据（双语）

**Files:**
- Create: `website/src/components/Reveal.tsx`, `website/src/data/copy.ts`

**Interfaces:**
- Consumes: 无新依赖（Reveal 用 IntersectionObserver，setup 已 mock）。
- Produces:
  - `Reveal({ children, as?, className? })`：进入视口前加 `reveal-hidden`，进入后加 `reveal-in`；无 IntersectionObserver 时直接可见。
  - `copy` 双语文案对象（`en` / `zh-CN`）、`INSTALL_CMD`、`heroTerminal: TermLine[]`、`heroTree: TreeRow[]`、`commandTabsData: CommandTab[]`、`workflowSteps`、`features`、`infraBadges`。供 `index.tsx` 消费。

- [ ] **Step 1: 实现 Reveal.tsx**

Create `website/src/components/Reveal.tsx`：

```tsx
import React, { useEffect, useRef, useState } from 'react';

type Props = {
  children: React.ReactNode;
  className?: string;
  delay?: number;
};

export default function Reveal({ children, className = '', delay = 0 }: Props) {
  const ref = useRef<HTMLDivElement | null>(null);
  const [inView, setInView] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el || typeof IntersectionObserver === 'undefined') {
      setInView(true);
      return;
    }
    const obs = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            setInView(true);
            obs.disconnect();
          }
        });
      },
      { threshold: 0.15 },
    );
    obs.observe(el);
    return () => obs.disconnect();
  }, []);

  return (
    <div
      ref={ref}
      className={`reveal ${inView ? 'reveal-in' : 'reveal-hidden'} ${className}`.trim()}
      style={delay ? { transitionDelay: `${delay}ms` } : undefined}
    >
      {children}
    </div>
  );
}
```

- [ ] **Step 2: 写 copy.ts（双语单一数据源）**

Create `website/src/data/copy.ts`：

```ts
import type { TermLine } from '../components/Terminal';
import type { TreeRow } from '../components/FileTree';
import type { CommandTab } from '../components/CommandTabs';

export const INSTALL_CMD = 'go install github.com/byx-darwin/ncgo@latest';

export const heroTerminal: TermLine[] = [
  { kind: 'command', text: 'ncgo new user-api --module github.com/acme/user-api' },
  { kind: 'output', text: '✔ .ncgo/manifest.yaml' },
  { kind: 'output', text: '✔ idl/app/user-api.proto' },
  { kind: 'output', text: '✔ template/' },
  { kind: 'success', text: '→ hz new: handler / service / repo generated' },
  { kind: 'highlight', text: '★ AGENTS.md    ★ CLAUDE.md' },
  { kind: 'highlight', text: '★ .claude/generated/project-context.md' },
];

export const heroTree: TreeRow[] = [
  { text: 'user-api/', depth: 0 },
  { text: '.ncgo/manifest.yaml', depth: 1 },
  { text: 'idl/app/user-api.proto', depth: 1 },
  { text: 'internal/handler/', depth: 1 },
  { text: 'internal/service/', depth: 1 },
  { text: 'internal/repo/', depth: 1 },
  { text: '.claude/generated/', depth: 1, ai: true },
  { text: 'AGENTS.md  CLAUDE.md', depth: 1, ai: true },
];

export const commandTabsData: CommandTab[] = [
  {
    id: 'doctor',
    label: 'doctor',
    lines: [
      { kind: 'command', text: 'ncgo doctor' },
      { kind: 'output', text: 'go      : ok (1.25)' },
      { kind: 'output', text: 'hz      : ok' },
      { kind: 'output', text: 'kitex   : ok' },
      { kind: 'success', text: 'project : manifest valid' },
    ],
  },
  {
    id: 'infra',
    label: 'add infra',
    lines: [
      { kind: 'command', text: 'ncgo add infra redis' },
      { kind: 'output', text: '✔ internal/infra/redis/' },
      { kind: 'success', text: 'redis helper wired into DI container' },
    ],
  },
  {
    id: 'mcp',
    label: 'mcp serve',
    lines: [
      { kind: 'command', text: 'ncgo mcp serve' },
      { kind: 'output', text: 'MCP stdio server listening' },
      { kind: 'highlight', text: 'tools: ncgo_version · ncgo_doctor · ncgo_ai_sync …' },
    ],
  },
];

export type WorkflowStep = { title: string; body: string };
export type Feature = { title: string; body: string; big?: boolean };

/** 基础设施徽章（产品名，中英一致，无需翻译） */
export const infraBadges = [
  'Redis', 'Kafka', 'Elasticsearch', 'ClickHouse', 'OpenTelemetry', 'Polaris', 'etcd',
];

export const copy = {
  en: {
    heroKicker: 'AI-friendly scaffold CLI',
    heroTitle: 'Scaffold Go microservices your agents can actually understand.',
    heroSub:
      'One command generates reproducible Hertz / Kitex service skeletons — with manifests, IDL placeholders, and AI context files under version control.',
    heroCtaDocs: 'Read the docs',
    heroCtaGithub: 'GitHub',
    workflowHeading: 'From one command to an agent-ready service',
    workflow: [
      { title: 'One command', body: 'ncgo new writes manifest, IDL placeholder and template inputs.' },
      { title: 'Generator runs', body: 'hz / kitex generate handler, service and repo layers.' },
      { title: 'AI context ready', body: 'AGENTS.md, CLAUDE.md and MCP tools are rendered alongside code.' },
      { title: 'Ship it', body: 'doctor, upgrade and extract keep the project healthy over time.' },
    ] as WorkflowStep[],
    featuresHeading: 'Built for reproducible, agent-friendly scaffolds',
    features: [
      { title: 'Agent-friendly by default', body: 'Renders AGENTS.md, CLAUDE.md, Cursor rules and exposes an MCP stdio server so AI tools understand every generated project.', big: true },
      { title: 'Deterministic scaffolding', body: 'Manifests and templates stay in version control — no one-off generator output.' },
      { title: 'Optional infra', body: 'Redis, Kafka, Elasticsearch, ClickHouse, observability and canary helpers.' },
      { title: 'Lifecycle tooling', body: 'doctor, upgrade, extract domain and protolint in one CLI.' },
    ] as Feature[],
    commandsHeading: 'See it in action',
    ctaHeading: 'Start in 30 seconds',
    footerNote: 'Built for the nc-skills-golang conventions.',
  },
  'zh-CN': {
    heroKicker: 'AI 友好的脚手架 CLI',
    heroTitle: '一条命令，生成 AI 能真正读懂的 Go 微服务骨架。',
    heroSub:
      '一次生成可复现的 Hertz / Kitex 服务骨架 —— manifest、IDL 占位符与 AI 上下文文件全部纳入版本控制。',
    heroCtaDocs: '阅读文档',
    heroCtaGithub: 'GitHub',
    workflowHeading: '从一条命令到 Agent 就绪的服务',
    workflow: [
      { title: '一条命令', body: 'ncgo new 写入 manifest、IDL 占位符与模板输入。' },
      { title: '生成器执行', body: 'hz / kitex 生成 handler、service、repo 分层。' },
      { title: 'AI 上下文就绪', body: 'AGENTS.md、CLAUDE.md 与 MCP 工具随代码一起渲染。' },
      { title: '持续演进', body: 'doctor、upgrade、extract 让项目长期保持健康。' },
    ] as WorkflowStep[],
    featuresHeading: '为可复现、对 Agent 友好的脚手架而生',
    features: [
      { title: '默认对 Agent 友好', body: '渲染 AGENTS.md、CLAUDE.md、Cursor 规则，并暴露 MCP stdio 服务器，让 AI 工具理解每个生成的项目。', big: true },
      { title: '确定性脚手架', body: 'manifest 与模板全部纳入版本控制，拒绝一次性生成器输出。' },
      { title: '可选基础设施', body: 'Redis、Kafka、Elasticsearch、ClickHouse、可观测与金丝雀发布助手。' },
      { title: '生命周期工具', body: 'doctor、upgrade、extract domain 与 protolint 集于一个 CLI。' },
    ] as Feature[],
    commandsHeading: '实际效果',
    ctaHeading: '30 秒上手',
    footerNote: '为 nc-skills-golang 约定而生。',
  },
} as const;

export type Locale = keyof typeof copy;

export function copyFor(locale: string) {
  return copy[(locale as Locale)] ?? copy.en;
}
```

- [ ] **Step 3: 追加 Reveal 样式**

在 `website/src/css/custom.css` 末尾追加：

```css
/* ── Reveal（滚动浮现） ───────────────────── */
.reveal { transition: opacity 0.6s ease, transform 0.6s ease; will-change: opacity, transform; }
.reveal-hidden { opacity: 0; transform: translateY(18px); }
.reveal-in { opacity: 1; transform: translateY(0); }
@media (prefers-reduced-motion: reduce) {
  .reveal { transition: none; }
  .reveal-hidden { opacity: 1; transform: none; }
}
```

- [ ] **Step 4: 类型检查**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run typecheck`
Expected: 无错误。

- [ ] **Step 5: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/src/components/Reveal.tsx website/src/data/copy.ts website/src/css/custom.css
git commit -m "feat(website): add Reveal component and bilingual landing copy data"
```

---

## Task 6: 落地页 index.tsx（组合所有 section）

**Files:**
- Create: `website/src/pages/index.tsx`
- Modify: `website/src/css/custom.css`（追加落地页 section 样式）

**Interfaces:**
- Consumes: `Terminal`、`FileTree`、`CommandTabs`、`CopyCommand`、`Reveal`（Task 3-5）；`copy`/`copyFor`/`heroTerminal`/`heroTree`/`commandTabsData`/`INSTALL_CMD`（Task 5）；Docusaurus 的 `Layout`、`Link`、`useDocusaurusContext`、`Translate`。
- Produces: 首页 `/`。版块顺序：Hero（左文案 + 右活终端/目录树）→ 工作流横轴 → 不对称 Bento 特性 → 命令速览 Tabs → 安装 CTA。页脚来自 theme 配置。

- [ ] **Step 1: 实现 index.tsx**

Create `website/src/pages/index.tsx`：

```tsx
import React from 'react';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import Translate from '@docusaurus/Translate';
import Terminal from '../components/Terminal';
import FileTree from '../components/FileTree';
import CommandTabs from '../components/CommandTabs';
import CopyCommand from '../components/CopyCommand';
import Reveal from '../components/Reveal';
import { copyFor, heroTerminal, heroTree, commandTabsData, infraBadges, INSTALL_CMD } from '../data/copy';

export default function Home(): React.JSX.Element {
  const { i18n } = useDocusaurusContext();
  const c = copyFor(i18n.currentLocale);

  return (
    <Layout description="AI-friendly scaffold CLI for Go microservices">
      <main className="landing">
        {/* Hero：左文案 + 右活终端/目录树（非居中三件套） */}
        <section className="hero-split">
          <div className="hero-copy">
            <span className="hero-kicker">{c.heroKicker}</span>
            <h1 className="hero-title">{c.heroTitle}</h1>
            <p className="hero-sub">{c.heroSub}</p>
            <CopyCommand command={INSTALL_CMD} />
            <div className="hero-ctas">
              <Link className="btn btn-primary" to="/docs/intro">{c.heroCtaDocs}</Link>
              <Link className="btn btn-ghost" to="https://github.com/byx-darwin/ncgo">{c.heroCtaGithub}</Link>
            </div>
          </div>
          <div className="hero-visual">
            <Terminal lines={heroTerminal} title="ncgo — scaffold" />
            <FileTree rows={heroTree} className="hero-tree" />
          </div>
        </section>

        {/* 工作流横轴 */}
        <section className="section">
          <Reveal><h2 className="section-heading">{c.workflowHeading}</h2></Reveal>
          <ol className="workflow">
            {c.workflow.map((s, i) => (
              <Reveal key={s.title} delay={i * 90}>
                <li className="workflow-step">
                  <span className="workflow-num">{String(i + 1).padStart(2, '0')}</span>
                  <h3>{s.title}</h3>
                  <p>{s.body}</p>
                </li>
              </Reveal>
            ))}
          </ol>
        </section>

        {/* 不对称 Bento 特性 */}
        <section className="section">
          <Reveal><h2 className="section-heading">{c.featuresHeading}</h2></Reveal>
          <div className="bento">
            {c.features.map((f, i) => (
              <Reveal key={f.title} delay={i * 70} className={f.big ? 'bento-big' : ''}>
                <div className="bento-card">
                  <h3>{f.title}</h3>
                  <p>{f.body}</p>
                </div>
              </Reveal>
            ))}
          </div>
        </section>

        {/* 基础设施集成带（横向滚动 marquee） */}
        <section className="section infra-band" aria-label="Supported infrastructure">
          <div className="marquee">
            <div className="marquee-track">
              {[...infraBadges, ...infraBadges].map((b, i) => (
                <span key={`${b}-${i}`} className="infra-badge">{b}</span>
              ))}
            </div>
          </div>
        </section>

        {/* 命令速览 Tabs */}
        <section className="section section-narrow">
          <Reveal><h2 className="section-heading">{c.commandsHeading}</h2></Reveal>
          <Reveal delay={100}><CommandTabs tabs={commandTabsData} /></Reveal>
        </section>

        {/* 安装 CTA */}
        <section className="section cta-band">
          <Reveal>
            <h2 className="cta-heading">{c.ctaHeading}</h2>
            <CopyCommand command={INSTALL_CMD} />
            <p className="cta-note">{c.footerNote}</p>
          </Reveal>
        </section>
      </main>
    </Layout>
  );
}
```

- [ ] **Step 2: 追加落地页 section 样式**

在 `website/src/css/custom.css` 末尾追加：

```css
/* ── Landing layout ───────────────────────── */
.landing { max-width: 1120px; margin: 0 auto; padding: 0 24px; }

.hero-split {
  display: grid;
  grid-template-columns: minmax(0, 5fr) minmax(0, 6fr);
  gap: 48px;
  align-items: center;
  padding: 72px 0 56px;
}
.hero-kicker {
  display: inline-block;
  font-family: var(--ifm-font-family-monospace);
  font-size: 12px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--ncgo-cyan);
  border: 1px solid var(--ncgo-cyan-soft);
  background: var(--ncgo-cyan-soft);
  border-radius: 999px;
  padding: 5px 14px;
  margin-bottom: 20px;
}
.hero-title {
  font-size: clamp(34px, 4.6vw, 52px);
  line-height: 1.08;
  font-weight: 700;
  color: var(--ncgo-text);
  margin: 0 0 18px;
}
.hero-sub {
  font-size: 17px;
  line-height: 1.65;
  color: var(--ncgo-text-dim);
  margin: 0 0 26px;
}
.hero-ctas { display: flex; gap: 12px; margin-top: 22px; }
.btn {
  display: inline-block;
  border-radius: 8px;
  padding: 10px 20px;
  font-weight: 600;
  font-size: 14.5px;
  text-decoration: none;
  transition: transform 0.15s ease, background 0.15s ease, border-color 0.15s ease;
}
.btn:hover { transform: translateY(-1px); text-decoration: none; }
.btn-primary { background: var(--ncgo-cyan); color: #04222c; }
.btn-primary:hover { background: #13c3ea; color: #04222c; }
.btn-ghost { border: 1px solid var(--ncgo-line); color: var(--ncgo-text); }
.btn-ghost:hover { border-color: var(--ncgo-cyan); color: var(--ncgo-cyan); }
.hero-visual { display: grid; gap: 18px; }

.section { padding: 56px 0; }
.section-narrow { max-width: 760px; margin: 0 auto; }
.section-heading {
  font-size: clamp(26px, 3vw, 34px);
  font-weight: 700;
  color: var(--ncgo-text);
  margin: 0 0 32px;
}

/* 工作流横轴 */
.workflow {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0;
  list-style: none;
  margin: 0;
  padding: 0;
  counter-reset: step;
}
.workflow .reveal { display: contents; }
.workflow-step {
  position: relative;
  padding: 20px 22px 20px 0;
}
.workflow-step::after {
  content: '';
  position: absolute;
  top: 34px;
  right: 12px;
  left: auto;
  width: calc(100% - 46px);
  height: 1px;
  background: linear-gradient(90deg, var(--ncgo-cyan), transparent);
  opacity: 0.5;
}
.workflow-step:last-child::after { display: none; }
.workflow-num {
  font-family: var(--ifm-font-family-monospace);
  color: var(--ncgo-cyan);
  font-size: 13px;
  display: block;
  margin-bottom: 10px;
}
.workflow-step h3 { font-size: 16px; margin: 0 0 8px; color: var(--ncgo-text); }
.workflow-step p { font-size: 13.5px; line-height: 1.6; color: var(--ncgo-text-dim); margin: 0; }

/* 不对称 Bento */
.bento {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
.bento .reveal { display: flex; }
.bento-big { grid-column: span 2; grid-row: span 2; }
.bento-card {
  flex: 1;
  border: 1px solid var(--ncgo-line);
  border-radius: 12px;
  background: color-mix(in srgb, var(--ncgo-ink-2) 90%, transparent);
  padding: 24px;
  transition: border-color 0.2s ease, transform 0.2s ease;
}
.bento-card:hover { border-color: var(--ncgo-cyan); transform: translateY(-3px); }
.bento-card h3 { font-size: 17px; color: var(--ncgo-text); margin: 0 0 10px; }
.bento-card p { font-size: 14px; line-height: 1.65; color: var(--ncgo-text-dim); margin: 0; }
.bento-big .bento-card { background: linear-gradient(140deg, var(--ncgo-cyan-soft), transparent 55%), color-mix(in srgb, var(--ncgo-ink-2) 90%, transparent); }
.bento-big .bento-card h3 { font-size: 22px; }

/* 基础设施 marquee */
.infra-band { padding: 8px 0 40px; overflow: hidden; }
.marquee {
  overflow: hidden;
  mask-image: linear-gradient(90deg, transparent, #000 12%, #000 88%, transparent);
  -webkit-mask-image: linear-gradient(90deg, transparent, #000 12%, #000 88%, transparent);
}
.marquee-track {
  display: inline-flex;
  gap: 14px;
  white-space: nowrap;
  animation: marquee-scroll 22s linear infinite;
}
.marquee:hover .marquee-track { animation-play-state: paused; }
.infra-badge {
  font-family: var(--ifm-font-family-monospace);
  font-size: 13px;
  color: var(--ncgo-text-dim);
  border: 1px solid var(--ncgo-line);
  border-radius: 999px;
  padding: 8px 18px;
  background: color-mix(in srgb, var(--ncgo-ink-2) 88%, transparent);
  transition: color 0.15s ease, border-color 0.15s ease;
}
.infra-badge:hover { color: var(--ncgo-cyan); border-color: var(--ncgo-cyan); }
@keyframes marquee-scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }
@media (prefers-reduced-motion: reduce) {
  .marquee-track { animation: none; }
}

/* CTA */
.cta-band { text-align: left; border-top: 1px solid var(--ncgo-line); padding-top: 64px; padding-bottom: 88px; }
.cta-heading { font-size: clamp(28px, 3.4vw, 40px); font-weight: 700; color: var(--ncgo-text); margin: 0 0 24px; }
.cta-note { margin-top: 18px; color: var(--ncgo-text-dim); font-size: 13.5px; }

/* 响应式 */
@media (max-width: 900px) {
  .hero-split { grid-template-columns: 1fr; padding-top: 48px; }
  .workflow { grid-template-columns: 1fr; }
  .workflow-step::after { display: none; }
  .bento { grid-template-columns: 1fr; }
  .bento-big { grid-column: span 1; grid-row: span 1; }
}
```

- [ ] **Step 3: 构建两个 locale 验证（含 SSR）**

Run:
```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website
npm run build
npm run build -- --locale zh-CN
```
Expected: 均 `SUCCESS`；`build/index.html` 中存在 `terminal` 与目录树文本（证明 SSG 预渲染了内容）。

- [ ] **Step 4: 本地预览检查**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run serve`
Expected: 打开 `http://localhost:3000/ncgo/` 显示落地页，终端自动打字，滚动有浮现；切换右上角语言到「简体中文」文案随之变化。`Ctrl+C` 结束。

- [ ] **Step 5: 类型检查 + 全量测试**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run typecheck && npm test`
Expected: 无类型错误；全部测试通过。

- [ ] **Step 6: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/src/pages/index.tsx website/src/css/custom.css
git commit -m "feat(website): compose landing page with hero terminal, workflow, bento, tabs, CTA"
```

---

## Task 7: EN 文档内容

**Files:**
- Create: `website/docs/getting-started/install.md`, `website/docs/getting-started/30-second-tour.md`, `website/docs/guides/{new-service,micro-workspace,domain-method,infra,ai-collaboration,diagnostics}.md`, `website/docs/reference/commands.md`, `website/docs/faq.md`
- Create: `website/docs/getting-started/_category_.json`, `website/docs/guides/_category_.json`, `website/docs/reference/_category_.json`

**Interfaces:**
- Consumes: Task 1 的 docs 结构（`intro.md` 已存在，slug `/intro`）。
- Produces: 完整 EN 文档树。每个 guide 的命令块来自真实 `ncgo <cmd> --help` 输出（见各 step 的捕获命令）。

- [ ] **Step 1: 写目录分组 _category_.json（3 个）**

Create `website/docs/getting-started/_category_.json`：
```json
{ "label": "Getting Started", "position": 2, "collapsed": false }
```
Create `website/docs/guides/_category_.json`：
```json
{ "label": "Guides", "position": 3, "collapsed": false }
```
Create `website/docs/reference/_category_.json`：
```json
{ "label": "Reference", "position": 4, "collapsed": false }
```

- [ ] **Step 2: 写 getting-started/install.md**

Create `website/docs/getting-started/install.md`：

```md
---
sidebar_position: 1
title: Install
---

# Install

## Prerequisites

- Go 1.25+
- `hz >= v0.9.7` (for Hertz flows)
- `kitex >= v0.16.1` (for Kitex flows)

## Install ncgo

```bash
go install github.com/byx-darwin/ncgo@latest
ncgo version
```

If `ncgo` is not found after installation, make sure your `GOBIN`
(or `$(go env GOPATH)/bin`) is on `PATH`.

From a local checkout, the repository root is also installable:

```bash
go install .
ncgo version
```

## Verify

Run the built-in diagnostics to confirm host tools are ready:

```bash
ncgo doctor
```
```

- [ ] **Step 3: 写 getting-started/30-second-tour.md**

Create `website/docs/getting-started/30-second-tour.md`：

```md
---
sidebar_position: 2
title: 30-Second Tour
---

# 30-Second Tour

Assuming `hz` is already on `PATH`, the shortest happy path is:

```bash
go install github.com/byx-darwin/ncgo@latest
ncgo new user-api --module github.com/acme/user-api
cd user-api
go mod tidy
make dev
```

`ncgo new` writes a manifest (`.ncgo/manifest.yaml`), an IDL placeholder,
template inputs, then invokes `hz` to generate the handler / service / repo
layers. AI context files (`AGENTS.md`, `CLAUDE.md`) are rendered alongside.

For Kitex, micro workspaces, and generator-free flows, see the
[guides](../guides/new-service.md).
```

- [ ] **Step 4: 捕获真实 flags 并写 guides/new-service.md**

先运行以下命令捕获真实输出（粘贴到文档的 flags 表格中）：

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo new --help`

Create `website/docs/guides/new-service.md`（将下方 `FLAGS_TABLE` 替换为上一步捕获的真实 flags 列表）：

```md
---
sidebar_position: 1
title: New Service
---

# New Service

`ncgo new` scaffolds a mono Hertz (HTTP) or Kitex (RPC) service.

## Hertz (HTTP)

```bash
ncgo new user-api --module github.com/acme/user-api --kind hertz
```

## Kitex (RPC)

```bash
ncgo new user-svc --module github.com/acme/user-svc --kind kitex
```

## Skip the generator

Write only the manifest, template inputs and IDL placeholder:

```bash
ncgo new user-api --module github.com/acme/user-api --no-generate
```

## Flags

FLAGS_TABLE  <!-- 用 `ncgo new --help` 的真实输出整理成 markdown 表格 -->

## What gets generated

- `.ncgo/manifest.yaml` — project metadata (single source of truth)
- `idl/` — IDL placeholder
- `template/` — template inputs for the generator
- handler / service / repo layers (via `hz` / `kitex`)
- AI context files (`AGENTS.md`, `CLAUDE.md`, `.claude/generated/`)
```

- [ ] **Step 5: 捕获并写 guides/micro-workspace.md**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo add rpc --help && ./ncgo add bff --help`

Create `website/docs/guides/micro-workspace.md`（flags 表格用捕获的真实输出）：

```md
---
sidebar_position: 2
title: Micro Workspace
---

# Micro Workspace

Create a micro workspace with a root `ncgo.workspace` plus services:

```bash
ncgo new shop --module github.com/acme/shop --mode micro
```

Add a Kitex RPC service:

```bash
ncgo add rpc user --root ./shop --module github.com/acme/shop/user
```

Add a Hertz BFF service:

```bash
ncgo add bff gateway --root ./shop --module github.com/acme/shop/gateway
```

FLAGS_TABLE  <!-- 用捕获的 `add rpc` / `add bff` --help 真实输出 -->
```

- [ ] **Step 6: 捕获并写 guides/domain-method.md**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo add domain --help && ./ncgo add method --help`

Create `website/docs/guides/domain-method.md`：

```md
---
sidebar_position: 3
title: Domain & Method
---

# Domain & Method

Generate usecase / repository / DI register files for a domain:

```bash
ncgo add domain order --root ./user-api
```

Insert a method stub at the ncgo anchor markers:

```bash
ncgo add method order.CreateOrder --root ./user-api --in usecase
```

FLAGS_TABLE  <!-- 用捕获的 --help 真实输出 -->
```

- [ ] **Step 7: 捕获并写 guides/infra.md**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo add infra --help`

Create `website/docs/guides/infra.md`：

```md
---
sidebar_position: 4
title: Infrastructure
---

# Infrastructure

`ncgo add infra` adds optional infra helpers to an existing project.

```bash
ncgo add infra redis --root ./user-api
```

Supported kinds include: `redis`, `kafka`, `es`, `clickhouse`,
`observability_otel` / `otel`, and (Kitex-only) `registry_etcd`.

FLAGS_TABLE  <!-- 用捕获的 --help 真实输出 -->
```

- [ ] **Step 8: 捕获并写 guides/ai-collaboration.md**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo ai --help && ./ncgo ai init claude --help && ./ncgo mcp serve --help`

Create `website/docs/guides/ai-collaboration.md`：

```md
---
sidebar_position: 5
title: AI Collaboration
---

# AI Collaboration

`ncgo` renders AI context files and exposes operations over MCP so coding
agents understand generated projects.

## Bootstrap .claude starter files

```bash
ncgo ai init claude --preset minimal
```

## Sync generated context

```bash
ncgo ai sync
```

Renders `AGENTS.md`, `CLAUDE.md`, `.claude/generated/project-context.md`, and
Cursor rules.

## Expose operations over MCP

```bash
ncgo mcp serve
```

Starts an MCP stdio server exposing tools such as `ncgo_version`,
`ncgo_doctor`, and `ncgo_ai_sync`.

FLAGS_TABLE  <!-- 用捕获的 --help 真实输出 -->
```

- [ ] **Step 9: 捕获并写 guides/diagnostics.md**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && ./ncgo doctor --help && ./ncgo upgrade --help && ./ncgo extract domain --help`

Create `website/docs/guides/diagnostics.md`：

```md
---
sidebar_position: 6
title: Diagnostics & Evolution
---

# Diagnostics & Evolution

## Diagnose

```bash
ncgo doctor
```

Checks host tools, project metadata, and default proto contract issues. Add
`--json` for machine-readable output.

## Upgrade metadata

```bash
ncgo upgrade --plan
```

Metadata-only MVP: previews manifest / assets metadata updates.

## Extract domain (mono → micro)

```bash
ncgo extract domain --plan
ncgo extract domain --apply
```

Conservative mono-to-micro extraction. `--plan` previews; `--apply` performs.

FLAGS_TABLE  <!-- 用捕获的 --help 真实输出 -->
```

- [ ] **Step 10: 写 reference/commands.md（真实顶层命令表）**

Create `website/docs/reference/commands.md`（顶层命令表来自 `ncgo --help` 真实输出）：

```md
---
sidebar_position: 1
title: Commands
---

# Command Reference

Top-level commands (from `ncgo --help`):

| Command | Purpose |
| --- | --- |
| `ncgo new` | Scaffold a new service or workspace |
| `ncgo add` | Add a feature (infra/domain/rpc/bff) to an existing project |
| `ncgo import` | Import an existing hz/kitex project into ncgo |
| `ncgo ai` | AI collaboration helpers (sync context, bootstrap .claude) |
| `ncgo doctor` | Diagnose host tools and the current project |
| `ncgo protolint` | Lint `.proto` files with Proto I/O rules |
| `ncgo i18n` | Inspect project i18n artifacts with structured output |
| `ncgo extract` | Plan or perform mono-to-micro extractions |
| `ncgo export` | Export code templates from an existing project |
| `ncgo upgrade` | Upgrade project metadata |
| `ncgo mcp` | MCP server for AI agents |
| `ncgo test` | Run generated tests for a project |
| `ncgo completion` | Generate shell completion script |
| `ncgo version` | Print ncgo, build, and assets versions |

For per-command flags, run `ncgo <command> --help`.
```

- [ ] **Step 11: 写 faq.md**

Create `website/docs/faq.md`：

```md
---
sidebar_position: 5
title: FAQ
---

# FAQ

### Do I need `hz` / `kitex` installed?

For generation flows, yes — `hz >= v0.9.7` for Hertz, `kitex >= v0.16.1` for
Kitex. Use `--no-generate` to skip the generator and only write the manifest,
template inputs, and IDL placeholder. Run `ncgo doctor` to check your toolchain.

### Is ncgo a framework?

No. `ncgo` is a scaffold and lifecycle CLI. It orchestrates the `hz` / `kitex`
generators and keeps manifests, templates, and AI context files under version
control.

### Can I use it on an existing project?

Yes. `ncgo import` generates a `.ncgo/manifest.yaml` for an existing Hertz or
Kitex project so subsequent `ncgo` commands work.

### Does it work with AI coding agents?

That is the point. `ncgo` renders `AGENTS.md`, `CLAUDE.md`, Cursor rules, and
exposes an MCP stdio server so agents can operate on generated projects.
```

- [ ] **Step 12: 构建验证（EN，断链 throw）**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run build`
Expected: `SUCCESS`，无 broken link（`onBrokenLinks: throw` 已生效）。

- [ ] **Step 13: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/docs
git commit -m "docs(website): add English docs (getting started, guides, reference, FAQ)"
```

---

## Task 8: zh-CN 文档翻译

**Files:**
- Create: `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/getting-started/{install,30-second-tour}.md`, `.../guides/{new-service,micro-workspace,domain-method,infra,ai-collaboration,diagnostics}.md`, `.../reference/commands.md`, `.../faq.md`, 以及对应 `_category_.json`

**Interfaces:**
- Consumes: Task 7 的 EN 文档树（结构与 frontmatter 镜像，`title` 与正文翻译为中文；命令块、路径、代码保持英文不变）。
- Produces: 完整 zh-CN 文档树，与 EN 一一对应。

- [ ] **Step 1: 镜像目录分组 _category_.json（3 个，label 译为中文）**

Create `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/getting-started/_category_.json`：
```json
{ "label": "快速开始", "position": 2, "collapsed": false }
```
Create `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/guides/_category_.json`：
```json
{ "label": "指南", "position": 3, "collapsed": false }
```
Create `website/i18n/zh-CN/docusaurus-plugin-content-docs/current/reference/_category_.json`：
```json
{ "label": "参考", "position": 4, "collapsed": false }
```

- [ ] **Step 2: 翻译 getting-started 两篇**

对 `install.md` 与 `30-second-tour.md`，复制 EN 文件到 zh 路径，保留 frontmatter 的 `sidebar_position`，将 `title` 与正文译为中文（`安装` / `30 秒上手`），**命令块、包路径、flag 保持英文原样**。

示例（`.../current/getting-started/install.md`）：

```md
---
sidebar_position: 1
title: 安装
---

# 安装

## 前置条件

- Go 1.25+
- `hz >= v0.9.7`（Hertz 流程）
- `kitex >= v0.16.1`（Kitex 流程）

## 安装 ncgo

```bash
go install github.com/byx-darwin/ncgo@latest
ncgo version
```

若安装后找不到 `ncgo`，请确认 `GOBIN`（或 `$(go env GOPATH)/bin`）已加入 `PATH`。

也可以从本地仓库根目录安装：

```bash
go install .
ncgo version
```

## 验证

运行内置诊断确认宿主工具就绪：

```bash
ncgo doctor
```
```

- [ ] **Step 3: 翻译 6 篇 guides**

逐一复制 EN guide 到 zh 对应路径并翻译（命令块/flag/路径保持英文）。标题对应：
`新建服务` / `微服务工作区` / `领域与方法` / `基础设施` / `AI 协作` / `诊断与演进`。frontmatter 的 `sidebar_position` 与 EN 保持一致。

- [ ] **Step 4: 翻译 reference/commands.md 与 faq.md**

标题：`命令参考` / `常见问题`。命令表左列命令保持英文，右列说明译为中文；FAQ 问题与回答译为中文，代码块保持英文。

- [ ] **Step 5: 构建验证（zh-CN，断链 throw）**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run build -- --locale zh-CN`
Expected: `SUCCESS`，无 broken link。

- [ ] **Step 6: 本地预览检查中文文档**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website && npm run serve -- --locale zh-CN`（先 `npm run build -- --locale zh-CN`）
Expected: 侧边栏为「快速开始 / 指南 / 参考」，文档为中文，命令块为英文。

- [ ] **Step 7: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add website/i18n
git commit -m "docs(website): add zh-CN translations of website docs"
```

---

## Task 9: GitHub Actions 部署工作流

**Files:**
- Create: `.github/workflows/website.yml`

**Interfaces:**
- Consumes: Task 1-8 的 `website/`（可构建）。
- Produces: push 到 `main` 且 `website/**` 变更 → 构建 + 部署 Pages；PR 且 `website/**` 变更 → 仅构建。不触碰 `ci.yml` / `release.yml`。

- [ ] **Step 1: 写 website.yml**

Create `.github/workflows/website.yml`：

```yaml
name: website

on:
  push:
    branches: [main]
    paths:
      - 'website/**'
      - '.github/workflows/website.yml'
  pull_request:
    paths:
      - 'website/**'
      - '.github/workflows/website.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: website
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Build (en + zh-CN)
        run: npm run build

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: website/build

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 校验 YAML 语法**

Run:
```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
node -e "const fs=require('fs');const yaml=require('/Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website/node_modules/js-yaml');yaml.load(fs.readFileSync('.github/workflows/website.yml','utf8'));console.log('YAML OK')"
```
Expected: 输出 `YAML OK`。（js-yaml 是 Docusaurus 的传递依赖，可直接复用；若路径不存在，改用 `npx --prefix website js-yaml .github/workflows/website.yml`。）

- [ ] **Step 3: 确认 Go CI 未被改动**

Run: `cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo && git status --short .github/workflows/ci.yml .github/workflows/release.yml`
Expected: 无输出（两个文件未被修改）。

- [ ] **Step 4: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add .github/workflows/website.yml
git commit -m "ci(website): add GitHub Pages build and deploy workflow"
```

- [ ] **Step 5: 提示用户在仓库 Settings 启用 Pages**

告知用户（不自动执行）：合并后需在 GitHub 仓库 **Settings → Pages → Source** 选择 **GitHub Actions**，部署才会生效。

---

## Task 10: 收尾验证 + README 链接 + 冒烟

**Files:**
- Modify: `README.md`, `README.zh-CN.md`（追加官网链接）

**Interfaces:**
- Consumes: 全部前序任务。
- Produces: 官网链接出现在 README；全量验证通过。

- [ ] **Step 1: 在 README.md 徽章区下追加官网链接**

在 `README.md` 顶部徽章段落之后插入一行：

```md
**Website:** [byx-darwin.github.io/ncgo](https://byx-darwin.github.io/ncgo/)
```

- [ ] **Step 2: 在 README.zh-CN.md 对应位置追加**

```md
**官网：** [byx-darwin.github.io/ncgo](https://byx-darwin.github.io/ncgo/)
```

- [ ] **Step 3: 网站全量验证**

Run:
```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo/website
npm run typecheck && npm test && npm run build && npm run build -- --locale zh-CN
```
Expected: 全部成功。

- [ ] **Step 4: Go 侧不受影响验证**

Run:
```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
go build ./... && go vet ./... && ./scripts/smoke.sh
```
Expected: 全部通过。

- [ ] **Step 5: Commit**

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
git add README.md README.zh-CN.md
git commit -m "docs: link project website from READMEs"
```

- [ ] **Step 6: 汇总验收清单（对照 Issue #26）**

逐项核对 Issue #26 的 Acceptance Criteria：
- `website/` 隔离、Go 构建/冒烟不受影响 ✅（Task 1/10 验证）
- 中英双语 + 导航语言切换 ✅（Task 1/8）
- 落地页各版块 ✅（Task 3-6）
- 字体/配色 ✅（Task 1/3-6，@fontsource 自托管）
- 文档区 + commands + FAQ 双语 ✅（Task 7/8）
- `.github/workflows/website.yml` ✅（Task 9）
- `npm run build` 两 locale + onBrokenLinks throw ✅（Task 1/7/8/10）
- 发布到 Pages（需用户启用 Settings → Pages → GitHub Actions，Task 9 Step 5）

---

## Verification Summary（执行完成后统一跑）

```bash
cd /Volumes/SSD/workspace/github.com/byx-darwin/ncgo
# 网站
cd website && npm run typecheck && npm test && npm run build && npm run build -- --locale zh-CN && cd ..
# Go（必须不受影响）
go build ./... && go vet ./... && go test ./... -count=1 && ./scripts/smoke.sh
```

全部通过即视为本计划完成。部署到线上需在仓库 Settings 启用 Pages（Source: GitHub Actions）后，由 `website.yml` 自动完成。
