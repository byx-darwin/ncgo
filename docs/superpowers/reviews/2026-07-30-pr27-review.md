# PR #27 Code Review Report

- **PR:** [#27 — feat(website): add Docusaurus-based bilingual project website](https://github.com/byx-darwin/ncgo/pull/27)
- **Branch:** `feat/26-project-website` → `main` (base `cd8e3d3`, head `5b584ba`)
- **Closes:** #26
- **Date:** 2026-07-30
- **Workflow:** gitflow-workflow contract `wf-2026-07-29-001` · Phase 4

> **Note on self-review:** This PR was created under the repository owner's account, so a
> formal GitHub *approve* is intentionally NOT submitted here (self-review is prohibited by
> the review policy and is disabled by GitHub for the PR author). This document records the
> independent review performed during execution; a human reviewer should perform the final
> GitHub approval/merge.

---

## Scope

58 files changed (+2,622 lines, excluding `package-lock.json`), 14 commits, building a
bilingual (EN + zh-CN) Docusaurus website under `website/` — fully isolated from the Go
codebase. Go code, `internal/assets/_data/`, and existing CI (`ci.yml` / `release.yml`)
are untouched.

## Review Method

Two-layer review during execution (subagent-driven-development):

1. **Per-task review** — every one of the 10 implementation tasks received a spec-compliance
   + code-quality review gate after implementation; the two TDD component tasks also went
   through a fix/re-review loop (Task 4: timer-leak + test-mock scoping; Task 1 placeholder
   typecheck fix).
2. **Whole-branch senior review** — a final broad review (most-capable model) over all 14
   commits, checking cross-cutting consistency, design fidelity, accessibility, SEO, and
   no-JS robustness.

## Verification (all green)

| Check | Result |
|-------|--------|
| `npm run typecheck` | ✅ clean |
| `npm test` (Vitest) | ✅ 12/12 |
| `npm run build` (en) | ✅ SUCCESS |
| `npm run build -- --locale zh-CN` | ✅ SUCCESS |
| `go build ./...` · `go vet ./...` | ✅ pass |
| `go test ./... -count=1` (27 pkgs) | ✅ pass |
| `./scripts/smoke.sh` | ✅ pass |
| Pre-push hooks | ✅ pass |
| GitHub Actions (branch) | ✅ 3/3 runs success, ~56s avg |

## Findings & Resolution

### Blocking (fixed before merge)

| # | Finding | Fix |
|---|---------|-----|
| 1 | Reveal-wrapped landing sections rendered `opacity:0` with JS disabled (violated design spec §4.6 graceful degradation) | `5b584ba`: added `.no-js .reveal-hidden { opacity:1; transform:none }` (Docusaurus `no-js`→`js` class swap) |
| 2 | `CopyCommand` leaked its 1.6s `setTimeout` on unmount (React warning) | `5444a5a`: ref-tracked timer cleared on unmount + before each new copy |
| 3 | Over-broad global `Object.defineProperty` interception in `vitest.setup.ts` | `5444a5a`: restored in `afterAll`, made non-silent (`console.debug`) |
| 4 | Placeholder `index.tsx` failed typecheck (`Layout` `title`/`description` props) | `7e9c156`: removed invalid props; homepage description from config `tagline` |

### Non-blocking (triaged SHIP AS-IS by whole-branch review)

- Unused `Translate` import in `index.tsx` — **removed** in `5b584ba`.
- `ncgo help` missing from command reference — **added** (EN + zh) in `5b584ba`.
- `.copy-command-btn:hover` bg equals base (transform-only affordance) — ship as-is.
- `.btn-primary` text/hover colors hardcoded (intentional high-contrast) — ship as-is.
- `sidebars.js` missing trailing semicolon — cosmetic, ship as-is.
- zh guide flag-table header style mirrors EN inconsistency — cosmetic, ship as-is.
- `reference/commands.md` omits Cobra `help` — resolved (added).

## Design Fidelity

The landing page faithfully realizes the approved design: terminal-led split hero (not a
centered trio), asymmetric feature bento (not equal cards), workflow axis with connectors,
infra marquee, interactive command tabs, three-typeface system (Chakra Petch / IBM Plex Sans
/ JetBrains Mono), deep-ink palette anchored on Go cyan `#00ADD8` with amber/green accents,
no indigo/violet/pink gradients or glassmorphism. Motion respects `prefers-reduced-motion`;
content renders with JS disabled after fix #1.

## Outstanding Actions (post-merge, user-side)

- Enable **Settings → Pages → Source: GitHub Actions** so the deploy job publishes the site.
- Human reviewer performs the formal GitHub approval + merge (self-review not submitted).

## Verdict

**Ready to merge.** All blocking issues fixed and verified; Go code and existing CI
untouched and green; design spec faithfully realized. Remaining items are cosmetic and
triaged as ship-as-is.
