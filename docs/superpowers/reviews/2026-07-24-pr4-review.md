# PR4 Code Review Report — 数据类 add-on 接入 go-middleware

- PR: https://github.com/byx-darwin/ncgo/pull/14
- Branch: `feat/9-data-addon-go-middleware`
- Date: 2026-07-24
- Reviewer: claude (automated)
- Verdict: **PASS**

## Diff Summary

13 files changed, 441 insertions(+), 357 deletions(-)

| Category | Files | Change |
|----------|-------|--------|
| Hertz templates | kafka.go, es.go, clickhouse.go | Rewrite to go-middleware |
| Kitex templates | kafka.go, es.go, clickhouse.go | Rewrite + error code alignment |
| YAML snippets | kafka.yaml, es.yaml, clickhouse.yaml | Field alignment to go-middleware tags |
| Go code | infra.go | goGetDeps update |
| Docs | README.md, README.zh-CN.md | go-middleware note |
| Plan | plan.md | Implementation plan (new) |

## Review Checklist

- [x] Diff is minimal and focused — only kafka/es/clickhouse migration
- [x] No unrelated cleanup or refactoring mixed in
- [x] Template wrapper pattern preserved (KafkaWriter/KafkaReader/ES/ClickHouse + samber/do DI)
- [x] go-middleware API usage correct (cross-referenced with go-middleware source)
  - kafka: `NewWriter(WriterConfig)` → `*Writer`, `NewConsumer(ReaderConfig)` → `*Consumer`
  - es: `NewClient(Config)` → `(*elasticsearch.Client, error)`
  - clickhouse: `NewClient(Config)` → `(clickhouse.Conn, error)`
- [x] Error code alignment correct per design doc
  - Hertz: `frameworkerror.CodeConfigInvalid` for config (unchanged)
  - Kitex: string codes → `frameworkerror.CodeConfigInvalid` (aligned)
  - ClickHouse: `CodeDatabaseUnavailable(40503)` deleted → `mwclickhouse.CodeConnect(20401)`
  - ES: `CodeSearchUnavailable(40506)` kept (go-middleware/es has no predefined codes)
- [x] Import paths correct — original libs kept only for type references (ES.Client, clickhouse.Conn)
- [x] YAML snippets match go-middleware Config yaml tags exactly
- [x] goGetDeps includes `go-framework` for kitex `frameworkerror` usage
- [x] Generated file markers and overwrite behavior unchanged
- [x] No MCP/CLI contract changes
- [x] No golden test changes needed (optional add-ons not in default generation)

## Validation Evidence

| Check | Result |
|-------|--------|
| `go build ./...` + `go build .` | ✅ Pass |
| `go vet ./...` | ✅ Pass |
| `gofmt -l` | ✅ Clean |
| `go test ./... -count=1` | ✅ All 27 packages pass |
| `./scripts/smoke.sh` | ✅ smoke OK |
| Pre-commit hooks | ✅ All pass |
| Pre-push hooks | ✅ All pass |
| CI pipeline | ✅ 2/2 checks SUCCESS |

## Risks / Notes

- **Breaking change for existing generated projects**: YAML field names changed (`brokers→broker`, `addr→addrs`, etc.). Accepted per design doc — ncgo tracks go-tools (clean cut decision).
- **ES/ClickHouse retain transitive imports**: `go-elasticsearch/v8` and `clickhouse-go/v2` still appear in imports for type references. `go get go-middleware` pulls them transitively.
