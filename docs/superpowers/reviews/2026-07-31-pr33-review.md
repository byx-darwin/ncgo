# Code Review Report — PR #33 (feat(infra): add infra polaris_adapter)

- **Date:** 2026-07-31
- **PR:** https://github.com/byx-darwin/ncgo/pull/33 · **Issue:** #32 · **Workflow:** wf-2026-07-31-001
- **Branch:** feat/32-polaris-adapter (merge-base ee012e2) · 9 commits
- **Review method:** subagent-driven-development — fresh implementer + task reviewer per task (7 tasks) + final whole-branch review.

## Verdict

**READY TO MERGE.** No Critical or Important findings. All binding constraints hold; end-to-end wiring consistent; tests/docs aligned.

## What was built

Opt-in `ncgo add infra polaris_adapter` (kitex-only MVP) wiring the real `polaris-go` SDK (tested v1.7.1) into ncgo's SDK-neutral canary seams (`PolarisInstanceLister`/`PolarisRuleLoader` → `release.Discoverer`/`RuleProvider`). New embedded asset `internal/assets/_data/kitex/optional/polaris_canary_adapter.go` (`package release`) with all polaris-go calls isolated behind a `polarisAPI` interface (`sdkClient`); env-only credentials; infra kind registered; MCP enum auto-includes it; compile-only SDK verification gate wired into CI; docs EN+ZH.

## Binding constraints (all verified)

| Constraint | Evidence |
|---|---|
| ncgo core imports no polaris-go | `grep -rn 'polarismesh/polaris-go' internal/ \| grep -v _data` → only string constants (dep list / next-steps / test assertion); zero compile imports |
| `release_canary.go` byte-identical | `git diff ee012e2..HEAD -- internal/assets/_data/optional/release_canary.go` empty |
| Default scaffold goldens unchanged | `git diff --stat ee012e2..HEAD -- internal/scaffold/{mono,rpc,bff,micro,domain}/testdata` empty |
| Credentials env-only, never leaked | `os.Getenv("POLARIS_TOKEN"/"POLARIS_NAMESPACE")` only; sanity test asserts no hardcoded-credential patterns; all `Public(...)` messages static literals |

## Coverage

| Seam | Test |
|---|---|
| Asset | `polaris_adapter_asset_test.go` (parse + symbols + no-creds) |
| Infra kind | `TestAddInfraPolarisAdapterKitex` / `RejectsHertz` / `Plan` (dry-run) |
| Golden | `TestGenerateGoldenInfraPolarisAdapter` (byte-identical to asset) |
| MCP enum | `server_test.go` membership assertion (`polaris_adapter` + alias) |
| SDK compile | `scripts/verify-polaris-adapter.sh` (runs green; pins polaris-go v1.7.1) |
| Repo-wide | `go build ./...`, `go vet ./...`, `go test ./... -count=1` (all pkgs), `./scripts/smoke.sh` — green |

## SDK API reconciliation (verified vs polaris-go@v1.7.1 source)

`config.NewDefaultConfiguration(addresses)`, `SetToken` (not `SetAuthToken`), `NewConsumerAPIByConfig`/`NewConfigAPIByConfig`, `GetAllInstances(req)` (no ctx), `GetConfigFile(ns,group,name)`, `ins.GetId()` — each confirmed against the cached SDK source; `consumer.Destroy()` cleanup on partial init.

## Minor findings (triaged — none must-fix)

- T1: `strings` import omitted (correct YAGNI — unused).
- T5: verify script needs network for `go mod tidy` (inherent; documented in script header).
- T5: `sdkClient` ctx param unused (deliberate forward-compat seam; polaris-go v1.7.1 takes no ctx).
- T6 (fixed during review): wrong dep `bytedance/go-common` → corrected to `byx-darwin/go-tools/go-common`; orphaned "Best for:" paragraph relocated.

## Follow-ups (non-blocking, recorded in Issue #32 / PR body)

- GA hardening (Phase B): decision metrics + logs, client cache + TTL, explicit discovery-error fail semantics, dry-run/shadow, runtime canary harness.
- Verify Kitex resolver returns full stable+canary instance set (resolver-visibility assumption).
- Nacos adapter (separate Issue).
