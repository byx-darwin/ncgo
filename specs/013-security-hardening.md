# 安全加固方案

- 状态：Draft
- 关联：`specs/010-v1.0-plan.md` Phase 1 / P0-3

## 1. MCP Frame 大小上限

**文件**：`internal/mcp/server.go` `readFrame()`

**改法**：MCP stdio 采用换行分隔 JSON（每行一条消息）。为防止恶意客户端发送超大单行导致内存耗尽，在按行读取时加上限检查：

```go
const maxFrameSize = 10 * 1024 * 1024 // 10 MB

func readFrame(r *bufio.Reader) ([]byte, error) {
    // 按 '\n' 读取单条消息；单行超过 maxFrameSize 则拒绝
    line, err := r.ReadString('\n')
    if len(line) > maxFrameSize {
        return nil, fmt.Errorf("mcp: frame %d bytes exceeds max %d", len(line), maxFrameSize)
    }
    // ...
}
```

## 2. MCP 路径沙箱

**文件**：`internal/mcp/` 所有接受 `root`/`dir` 参数的工具 handler

**改法**：在 `internal/mcp/` 加共享校验函数：

```go
// validateProjectRoot 确保操作不逃逸出工作目录
func validateProjectRoot(target, cwd string) error {
    abs, err := filepath.Abs(target)
    if err != nil {
        return fmt.Errorf("mcp: cannot resolve path %q: %w", target, err)
    }
    absCwd, _ := filepath.Abs(cwd)
    if !strings.HasPrefix(abs, absCwd) {
        return fmt.Errorf("mcp: path %q is outside the workspace %q", abs, absCwd)
    }
    return nil
}
```

每个工具 handler 在处理 `root`/`dir` 参数后立即调用。

## 3. compose.yaml 生产警告

**文件**：`internal/scaffold/shared/container.go` `writeMonoCompose()` / `writeMicroCompose()`

**改法**：生成文件开头加醒目的注释块：

```yaml
# ============================================================================
# WARNING: This file is for LOCAL DEVELOPMENT ONLY.
# All infrastructure services below have authentication DISABLED by default.
#   - PostgreSQL: postgres/postgres
#   - etcd: ALLOW_NONE_AUTHENTICATION=yes
#   - Nacos: NACOS_AUTH_ENABLE=false
#   - Elasticsearch: xpack.security.enabled=false
#   - ClickHouse: empty password
#   - Kafka: PLAINTEXT listeners
# NEVER use this compose.yaml in production or on a publicly accessible host.
# ============================================================================
```

## 4. `.dockerignore` 敏感文件

**文件**：`internal/scaffold/shared/container.go` `serviceDockerIgnore` 常量

**改法**：追加敏感文件模式：

```go
const serviceDockerIgnore = `# Generated by ncgo.
.git/
.idea/
.vscode/
output/
tmp/
vendor/
template/
.ncgo/
scripts/
# Security: never include secrets or credentials in images
.env
.env.*
*.pem
*.key
id_*
credentials
credentials.*
.aws/
.gcloud/
*.local.yaml
secrets/
`
```

## 5. Module Path 校验

**文件**：`internal/scaffold/mono/mono.go` `validate()` 方法

**改法**：从 `strings.Contains("/")` 升级为正则：

```go
var modulePathRE = regexp.MustCompile(
    `^[a-zA-Z][a-zA-Z0-9.-]*(\.[a-zA-Z][a-zA-Z0-9.-]*)+/[a-zA-Z][a-zA-Z0-9._/-]*$`,
)

func validateModulePath(module string) error {
    if !modulePathRE.MatchString(module) {
        return fmt.Errorf("invalid Go module path %q; expected form like github.com/acme/myapp", module)
    }
    return nil
}
```

## 6. Proto 文件大小限制（可选，Medium）

**文件**：`internal/protolint/load.go` `Load()`

```go
const maxProtoSize = 50 * 1024 * 1024 // 50 MB
```

加载前 `os.Stat` 检查大小，超过上限返回明确错误。

## 影响范围

| 改动 | 文件 | 风险 |
|------|------|------|
| Frame 上限 | `server.go` +3 行 | 极低，纯防御 |
| 路径沙箱 | 8 个 MCP tool handler | 低，不改变正常路径行为 |
| compose 警告 | `container.go` +10 行注释 | 极低，仅注释文本 |
| dockerignore | `container.go` +12 行 | 低，仅追加排除项 |
| Module 校验 | `mono.go` | 中，可能拒绝之前接受的边缘 case |
| Proto 大小 | `load.go` +5 行 | 极低 |
