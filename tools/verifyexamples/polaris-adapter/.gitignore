# Generated by scripts/verify-polaris-adapter.sh from embedded assets.
# Do not commit — the script copies + (if needed) substitutes the adapter
# and canary seam assets into this directory at verification time.
release/
